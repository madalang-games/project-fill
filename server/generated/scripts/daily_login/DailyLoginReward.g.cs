// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/daily_login/daily_login_reward.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;
using ProjectFill.Contracts.GameTypes;

namespace ProjectFill.Generated.Data
{
    public sealed class DailyLoginReward
    {
        public int id { get; set; }
        public AttendanceCycleType cycle_type { get; set; }
        public int day { get; set; }
        public int reward_group_id { get; set; }
        public int sort_order { get; set; }
    }

    public static class DailyLoginRewardLoader
    {
        public static IReadOnlyList<DailyLoginReward> LoadAll(string csvPath)
        {
            var result = new List<DailyLoginReward>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new DailyLoginReward
                {
                    id = idx.TryGetValue("id", out var i0) && i0 < cols.Length && !string.IsNullOrEmpty(cols[i0]) ? int.Parse(cols[i0]) : default,
                    cycle_type = idx.TryGetValue("cycle_type", out var i1) && i1 < cols.Length && !string.IsNullOrEmpty(cols[i1]) ? Enum.Parse<AttendanceCycleType>(cols[i1]) : default,
                    day = idx.TryGetValue("day", out var i2) && i2 < cols.Length && !string.IsNullOrEmpty(cols[i2]) ? int.Parse(cols[i2]) : default,
                    reward_group_id = idx.TryGetValue("reward_group_id", out var i3) && i3 < cols.Length && !string.IsNullOrEmpty(cols[i3]) ? int.Parse(cols[i3]) : default,
                    sort_order = idx.TryGetValue("sort_order", out var i4) && i4 < cols.Length && !string.IsNullOrEmpty(cols[i4]) ? int.Parse(cols[i4]) : default,
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<int, DailyLoginReward> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}