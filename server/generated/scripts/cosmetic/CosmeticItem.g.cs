// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/cosmetic/cosmetic_item.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;
using ProjectFill.Contracts.GameTypes;

namespace ProjectFill.Generated.Data
{
    public sealed class CosmeticItem
    {
        public string cosmetic_id { get; set; } = "";
        public CosmeticCategory category { get; set; }
        public string name_key { get; set; } = "";
        public string desc_key { get; set; } = "";
        public CosmeticUnlockType unlock_type { get; set; }
        public int unlock_cost { get; set; }
        public string unlock_condition_id { get; set; } = "";
        public string preview_res { get; set; } = "";
        public int sort_order { get; set; }
    }

    public static class CosmeticItemLoader
    {
        public static IReadOnlyList<CosmeticItem> LoadAll(string csvPath)
        {
            var result = new List<CosmeticItem>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new CosmeticItem
                {
                    cosmetic_id = idx.TryGetValue("cosmetic_id", out var i0) && i0 < cols.Length ? (cols[i0] ?? "") : "",
                    category = idx.TryGetValue("category", out var i1) && i1 < cols.Length && !string.IsNullOrEmpty(cols[i1]) ? Enum.Parse<CosmeticCategory>(cols[i1]) : default,
                    name_key = idx.TryGetValue("name_key", out var i2) && i2 < cols.Length ? (cols[i2] ?? "") : "",
                    desc_key = idx.TryGetValue("desc_key", out var i3) && i3 < cols.Length ? (cols[i3] ?? "") : "",
                    unlock_type = idx.TryGetValue("unlock_type", out var i4) && i4 < cols.Length && !string.IsNullOrEmpty(cols[i4]) ? Enum.Parse<CosmeticUnlockType>(cols[i4]) : default,
                    unlock_cost = idx.TryGetValue("unlock_cost", out var i5) && i5 < cols.Length && !string.IsNullOrEmpty(cols[i5]) ? int.Parse(cols[i5]) : default,
                    unlock_condition_id = idx.TryGetValue("unlock_condition_id", out var i6) && i6 < cols.Length ? (cols[i6] ?? "") : "",
                    preview_res = idx.TryGetValue("preview_res", out var i7) && i7 < cols.Length ? (cols[i7] ?? "") : "",
                    sort_order = idx.TryGetValue("sort_order", out var i8) && i8 < cols.Length && !string.IsNullOrEmpty(cols[i8]) ? int.Parse(cols[i8]) : default,
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<string, CosmeticItem> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.cosmetic_id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}