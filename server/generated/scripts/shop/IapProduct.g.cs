// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/shop/iap_product.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;
using ProjectFill.Contracts.GameTypes;

namespace ProjectFill.Generated.Data
{
    public sealed class IapProduct
    {
        public int info_id { get; set; }
        public string store_product_id { get; set; } = "";
        public IapProductType product_type { get; set; }
        public string name_key { get; set; } = "";
        public string desc_key { get; set; } = "";
        public double price_usd { get; set; }
        public string icon_res { get; set; } = "";
        public int reward_group_id { get; set; }
        public int purchase_limit { get; set; }
        public PurchaseResetPeriod reset_period { get; set; }
        public int sort_order { get; set; }
        public bool is_enabled { get; set; }
    }

    public static class IapProductLoader
    {
        public static IReadOnlyList<IapProduct> LoadAll(string csvPath)
        {
            var result = new List<IapProduct>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new IapProduct
                {
                    info_id = idx.TryGetValue("info_id", out var i0) && i0 < cols.Length && !string.IsNullOrEmpty(cols[i0]) ? int.Parse(cols[i0]) : default,
                    store_product_id = idx.TryGetValue("store_product_id", out var i1) && i1 < cols.Length ? (cols[i1] ?? "") : "",
                    product_type = idx.TryGetValue("product_type", out var i2) && i2 < cols.Length && !string.IsNullOrEmpty(cols[i2]) ? Enum.Parse<IapProductType>(cols[i2]) : default,
                    name_key = idx.TryGetValue("name_key", out var i3) && i3 < cols.Length ? (cols[i3] ?? "") : "",
                    desc_key = idx.TryGetValue("desc_key", out var i4) && i4 < cols.Length ? (cols[i4] ?? "") : "",
                    price_usd = idx.TryGetValue("price_usd", out var i5) && i5 < cols.Length && !string.IsNullOrEmpty(cols[i5]) ? double.Parse(cols[i5], System.Globalization.CultureInfo.InvariantCulture) : default,
                    icon_res = idx.TryGetValue("icon_res", out var i6) && i6 < cols.Length ? (cols[i6] ?? "") : "",
                    reward_group_id = idx.TryGetValue("reward_group_id", out var i7) && i7 < cols.Length && !string.IsNullOrEmpty(cols[i7]) ? int.Parse(cols[i7]) : default,
                    purchase_limit = idx.TryGetValue("purchase_limit", out var i8) && i8 < cols.Length && !string.IsNullOrEmpty(cols[i8]) ? int.Parse(cols[i8]) : default,
                    reset_period = idx.TryGetValue("reset_period", out var i9) && i9 < cols.Length && !string.IsNullOrEmpty(cols[i9]) ? Enum.Parse<PurchaseResetPeriod>(cols[i9]) : default,
                    sort_order = idx.TryGetValue("sort_order", out var i10) && i10 < cols.Length && !string.IsNullOrEmpty(cols[i10]) ? int.Parse(cols[i10]) : default,
                    is_enabled = idx.TryGetValue("is_enabled", out var i11) && i11 < cols.Length && cols[i11] == "true",
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<int, IapProduct> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.info_id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}