// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/reward/reward_source.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;

namespace ProjectFill.Generated.Data
{
    public sealed class RewardSource
    {
        public int id { get; set; }
        public string source_key { get; set; } = "";
        public string source_type { get; set; } = "";
        public int reward_group_id { get; set; }
        public int version { get; set; }
        public string claim_policy { get; set; } = "";
        public int max_claims { get; set; }
        public string reset_timezone { get; set; } = "";
        public int reset_hour { get; set; }
        public string ui_surface { get; set; } = "";
        public bool is_enabled { get; set; }
    }

    public static class RewardSourceLoader
    {
        public static IReadOnlyList<RewardSource> LoadAll(string csvPath)
        {
            var result = new List<RewardSource>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new RewardSource
                {
                    id = idx.TryGetValue("id", out var i0) && i0 < cols.Length && !string.IsNullOrEmpty(cols[i0]) ? int.Parse(cols[i0]) : default,
                    source_key = idx.TryGetValue("source_key", out var i1) && i1 < cols.Length ? (cols[i1] ?? "") : "",
                    source_type = idx.TryGetValue("source_type", out var i2) && i2 < cols.Length ? (cols[i2] ?? "") : "",
                    reward_group_id = idx.TryGetValue("reward_group_id", out var i3) && i3 < cols.Length && !string.IsNullOrEmpty(cols[i3]) ? int.Parse(cols[i3]) : default,
                    version = idx.TryGetValue("version", out var i4) && i4 < cols.Length && !string.IsNullOrEmpty(cols[i4]) ? int.Parse(cols[i4]) : default,
                    claim_policy = idx.TryGetValue("claim_policy", out var i5) && i5 < cols.Length ? (cols[i5] ?? "") : "",
                    max_claims = idx.TryGetValue("max_claims", out var i6) && i6 < cols.Length && !string.IsNullOrEmpty(cols[i6]) ? int.Parse(cols[i6]) : default,
                    reset_timezone = idx.TryGetValue("reset_timezone", out var i7) && i7 < cols.Length ? (cols[i7] ?? "") : "",
                    reset_hour = idx.TryGetValue("reset_hour", out var i8) && i8 < cols.Length && !string.IsNullOrEmpty(cols[i8]) ? int.Parse(cols[i8]) : default,
                    ui_surface = idx.TryGetValue("ui_surface", out var i9) && i9 < cols.Length ? (cols[i9] ?? "") : "",
                    is_enabled = idx.TryGetValue("is_enabled", out var i10) && i10 < cols.Length && cols[i10] == "true",
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<int, RewardSource> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}