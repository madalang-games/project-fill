// AUTO-GENERATED by gen:data — do not edit
// Source: shared/datas/achievement/achievement.csv
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;
using ProjectFill.Contracts.GameTypes;

namespace ProjectFill.Generated.Data
{
    public sealed class Achievement
    {
        public string achievement_id { get; set; } = "";
        public AchievementCategory category { get; set; }
        public string name_key { get; set; } = "";
        public string desc_key { get; set; } = "";
        public AchievementTier tier { get; set; }
        public int reward_group_id { get; set; }
        public AchievementConditionType condition_type { get; set; }
        public int condition_value { get; set; }
        public int sort_order { get; set; }
    }

    public static class AchievementLoader
    {
        public static IReadOnlyList<Achievement> LoadAll(string csvPath)
        {
            var result = new List<Achievement>();
            if (!File.Exists(csvPath)) return result;
            using var reader = new StreamReader(csvPath);
            var headerLine = reader.ReadLine();
            if (headerLine == null) return result;
            var headers = SplitCsvLine(headerLine);
            var idx = new Dictionary<string, int>(StringComparer.Ordinal);
            for (int i = 0; i < headers.Length; i++) idx[headers[i]] = i;
            string? line;
            while ((line = reader.ReadLine()) is not null)
            {
                if (string.IsNullOrWhiteSpace(line)) continue;
                var cols = SplitCsvLine(line);
                result.Add(new Achievement
                {
                    achievement_id = idx.TryGetValue("achievement_id", out var i0) && i0 < cols.Length ? (cols[i0] ?? "") : "",
                    category = idx.TryGetValue("category", out var i1) && i1 < cols.Length && !string.IsNullOrEmpty(cols[i1]) ? Enum.Parse<AchievementCategory>(cols[i1]) : default,
                    name_key = idx.TryGetValue("name_key", out var i2) && i2 < cols.Length ? (cols[i2] ?? "") : "",
                    desc_key = idx.TryGetValue("desc_key", out var i3) && i3 < cols.Length ? (cols[i3] ?? "") : "",
                    tier = idx.TryGetValue("tier", out var i4) && i4 < cols.Length && !string.IsNullOrEmpty(cols[i4]) ? Enum.Parse<AchievementTier>(cols[i4]) : default,
                    reward_group_id = idx.TryGetValue("reward_group_id", out var i5) && i5 < cols.Length && !string.IsNullOrEmpty(cols[i5]) ? int.Parse(cols[i5]) : default,
                    condition_type = idx.TryGetValue("condition_type", out var i6) && i6 < cols.Length && !string.IsNullOrEmpty(cols[i6]) ? Enum.Parse<AchievementConditionType>(cols[i6]) : default,
                    condition_value = idx.TryGetValue("condition_value", out var i7) && i7 < cols.Length && !string.IsNullOrEmpty(cols[i7]) ? int.Parse(cols[i7]) : default,
                    sort_order = idx.TryGetValue("sort_order", out var i8) && i8 < cols.Length && !string.IsNullOrEmpty(cols[i8]) ? int.Parse(cols[i8]) : default,
                });
            }
            return result;
        }

        public static IReadOnlyDictionary<string, Achievement> LoadAsDict(string csvPath)
            => LoadAll(csvPath).ToDictionary(r => r.achievement_id);

        private static string[] SplitCsvLine(string line)
        {
            var result = new List<string>();
            int i = 0;
            while (true)
            {
                if (i < line.Length && line[i] == '"')
                {
                    i++;
                    var sb = new System.Text.StringBuilder();
                    while (i < line.Length)
                    {
                        if (line[i] == '"')
                        {
                            if (i + 1 < line.Length && line[i + 1] == '"') { sb.Append('"'); i += 2; }
                            else { i++; break; }
                        }
                        else sb.Append(line[i++]);
                    }
                    result.Add(sb.ToString());
                }
                else
                {
                    int start = i;
                    while (i < line.Length && line[i] != ',') i++;
                    result.Add(line.Substring(start, i - start));
                }
                if (i >= line.Length) break;
                i++;
            }
            return result.ToArray();
        }
    }
}